class Person: #class and object
  def __init__(self, name, age): #defining properties of object as self name and age
    self.name = name #defines the name
    self.age = age #defines the age

p1 = Person("Salma", 17) #creating the class properties
def greet(self):
   print("Hello, my name is "Salma" + self.name")
P1=Person("Salma")
p1.greet()
print(p1.name) # prints name
print(p1.age) # prints age
